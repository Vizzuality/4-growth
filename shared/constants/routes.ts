const ROUTES = {
  surveyAnalysis: {
    explore: 'survey-analysis',
    sandbox: 'survey-analysis/sandbox',
  },
  projections: {
    explore: 'projections',
    sandbox: 'projections/sandbox',
  },
} as const;

export default ROUTES;
