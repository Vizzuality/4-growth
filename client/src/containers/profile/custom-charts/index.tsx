"use client";
import { FC } from "react";

import { Card, CardTitle } from "@/components/ui/card";

const UserCharts: FC = () => {
  return (
    <Card className="h-full px-0">
      <CardTitle className="pl-8">My custom charts</CardTitle>
      {/* // todo */}
    </Card>
  );
};

export default UserCharts;
